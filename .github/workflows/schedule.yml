name: Daily Sync

on:
  schedule:
    - cron: '50 15 * * *' # 台灣時間 23:50
  workflow_dispatch: # 允許手動測試

jobs:
  sync:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          # 👇👇👇 終極解法：在這裡就掛載你的萬能鑰匙 👇👇👇
          token: ${{ secrets.GH_PAT }}
          persist-credentials: true
          fetch-depth: 0 # 完整下載 Git 歷史紀錄，避免 Commit 報錯
          # 👆👆👆👆👆👆👆👆👆👆👆👆👆👆👆👆👆👆👆

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install msal requests GitPython

      - name: Run Daily Sync Script
        env:
          MS_CLIENT_ID: ${{ secrets.MS_CLIENT_ID }}
          MS_CLIENT_SECRET: ${{ secrets.MS_CLIENT_SECRET }}
          MS_TENANT_ID: ${{ secrets.MS_TENANT_ID }}
          MS_REFRESH_TOKEN: ${{ secrets.MS_REFRESH_TOKEN }}
          # 這裡不需要傳 GH_PAT 了，因為上面 Checkout 已經驗證過了
        run: python daily_sync.py

      - name: Keepalive Workflow
        uses: gautamkrishnar/keepalive-workflow@v1
        with:
          time_elapsed: 50
